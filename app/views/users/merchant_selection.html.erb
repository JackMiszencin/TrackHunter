<%= form_for @current_user, :html => { :class => 'form-horizontal' } do |f| %>   

<div class="control-group">
	<%=f.label "Merchant", :class => 'control-label' %>
	<div class="controls">

    <%= collection_select(:current_user, :merchant_id, @merchants, :id, :name, options={:prompt => "select a store", :include_blank => "Select your location"}, :class => "merchant")%>
   </div>
 </div> 
   <div class="form-actions">
   <%= submit_tag "Update Store", :class => "form_submit", :name => "updated_store" %> 
   <%= link_to t('.cancel', :default => t("helpers.links.cancel")), new_rating_path, :class => 'btn' %>
</div>
<% end %>   